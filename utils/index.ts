// import module

import { getArray, getBlendArray, LowerCase, UpperCase } from './array';

// log helper
const logger = () => console.table(Object.keys(module.exports));

export { getArray, getBlendArray, LowerCase, UpperCase, logger };
